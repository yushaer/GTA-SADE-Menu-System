{
  "name": "trainer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "watch": {
    "build": {
      "patterns": [
        "src"
      ],
      "extensions": "js"
    }
  },
  "scripts": {
    "watch": "npm-watch",
    "dev": "watch 'npm run build' src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d \"GTA San Andreas - Definitive Edition\"\\Gameface\\Binaries\\Win64\\CLEO"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "npm-watch": "^0.11.0"
  },
  "devDependencies": {
    "watch": "^1.0.2"
  }
}
